#include<bitset>/stdc++.h>
using namespace std;
int partition(int arr[], int s, int e) {
  int partition_value = arre];
  int j = s-1;
  for(int i = s; i < e; i++) {
    if(arr[i] <= partition_value) {
      
    }
  }
}